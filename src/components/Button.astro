---
import { cn } from "@/utils/cn"

export type Props = astroHTML.JSX.ButtonHTMLAttributes

const { class: className, ...props } = Astro.props
---

<button class={cn("rounded-full  px-4 py-2 flex gap-2 justify-center", className)} {...props}>
	<slot /></button
>
